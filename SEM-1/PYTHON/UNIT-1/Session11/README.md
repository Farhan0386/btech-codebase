# 🧵 Session-11: Strings in Python

📘 Unit-1  Programming for Problem Solving Using Python

🔍 Overview
This session introduces Python strings, focusing on their structure, immutability, and core operations. It covers string creation, indexing, slicing, concatenation, escape sequences, formatting techniques, and essential built-in methods.

🧠 Key Concepts
📌 String Fundamentals

- A string is a sequence of characters enclosed in quotes.
- Strings are immutable in Python.
- Quotes can be single (' '), double (" "), or triple (''' ''' / """ """).

🔢 Indexing & Slicing

- Strings are zero-indexed.
- Indexing retrieves individual characters.
- Slicing extracts substrings using the format: string[start:end:step].
➕ Concatenation
- Combine strings using the + operator or the join() method.
- Concatenation creates a new string.

🔡 Escape Characters

- Special characters prefixed with \ to represent whitespace, quotes, or control characters.
- Common escape sequences include newline, tab, backslash, etc.

🧩 String Formatting

- Format strings dynamically using:
- format() method (positional or keyword arguments)
- f-strings (formatted string literals)

🛠️ Built-in String Methods

- Case conversion: upper(), lower(), capitalize()
- Whitespace handling: strip(), lstrip(), rstrip()
- Substring operations: find(), index(), replace(), count()
- Splitting and joining: split(), join()
- Validation: isalnum(), isalpha(), startswith(), endswith()
